"use client";

// Admin view has full access - same as Manager but with all permissions
// For now, we'll use the Manager view as the base
export { TaskDetailsViewManager as TaskDetailsViewAdmin } from "./TaskDetailsViewManager";

